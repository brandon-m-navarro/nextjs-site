import { Metadata } from "next";

export const metadata: Metadata = {
  title: 'Brandon Manuel Navarro',
};

export default function ResumePage() {
  return (
    <div>
        TEST
    </div>
  );
}
